object Solution {
    def removeDuplicates(nums: Array[Int]): Int = {
        var i = 0;
        for(element <- nums){
            if(element != nums(i)){
                i += 1;
                nums(i) = element; 
            }
        }
        i+1;
    }
}
